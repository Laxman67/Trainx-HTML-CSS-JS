// HOF Can be called in two ways if funciton is returning another function
// 1.
// 2.

/**
 *This Function greet the user and accepts username parameter
 * @param {string} username - name of the user that calling the function
 * @param {number} [age]
 * @example
 * const user = 90
 */
function greet(username, age) {}

greet();
